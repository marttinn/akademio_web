<h1> Courses List </h1>

<div class="row">
	{{#each model as |course|}}
		<div class="col s12 m6 l6">
			<div class="card">
				<div class="card-content">
					<span class="card-title">	  		
						<h3>{{course.title}}</h3>
					</span>
					<p>Some day I´m going to be a real card description, but for now I´m a useless stub =(.</p>
				</div>
			</div>
		</div>
	{{/each}}
</div>
